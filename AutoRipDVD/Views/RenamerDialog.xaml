<ContentDialog
    x:Class="AutoRipDVD.Views.RenamerDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:AutoRipDVD.Views"
    xmlns:models="using:AutoRipDVD.Models"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Rename / Match Files"
    PrimaryButtonText="Apply"
    CloseButtonText="Cancel">

    <StackPanel Spacing="12" Padding="12">
        <TextBlock Text="Choose metadata source and options for renaming/matching files."/>

        <StackPanel>
            <TextBlock Text="Source:" FontWeight="SemiBold"/>
            <ComboBox x:Name="SourceCombo" SelectedIndex="0">
                <ComboBoxItem Content="TMDb"/>
                <ComboBoxItem Content="OMDb"/>
                <ComboBoxItem Content="TVDB"/>
                <ComboBoxItem Content="IMDb (requires key)"/>
            </ComboBox>
        </StackPanel>

        <StackPanel>
            <TextBlock Text="Available API Keys:" FontWeight="SemiBold"/>
            <TextBlock x:Name="KeysText" Text="Checking..." TextWrapping="Wrap"/>
            <TextBlock x:Name="ApiStatusText" Text="" Foreground="Gray"/>
        </StackPanel>

        <StackPanel>
            <TextBlock Text="Select Folder To Preview Files:" FontWeight="SemiBold"/>
            <StackPanel Orientation="Horizontal" Spacing="8">
                <TextBox x:Name="SelectedFolderBox" Width="420" IsReadOnly="True"/>
                <Button x:Name="SelectFolderBtn" Click="SelectFolderBtn_Click">
                    <StackPanel Orientation="Horizontal" Spacing="6">
                        <FontIcon Glyph="&#xE8B7;"/>
                        <TextBlock Text="Browse"/>
                    </StackPanel>
                </Button>
            </StackPanel>
        </StackPanel>


        <Grid ColumnSpacing="12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="3*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="6" Padding="8" MaxHeight="300">
                <ListView x:Name="FilesListView" SelectionMode="Multiple" SelectionChanged="FilesListView_SelectionChanged" IsItemClickEnabled="False"/>
            </Border>

            <StackPanel Grid.Column="1" Spacing="8">
                <TextBlock Text="Match candidates" FontWeight="SemiBold"/>
                <ListView x:Name="CandidatesListView" SelectionChanged="CandidatesListView_SelectionChanged" Height="160">
                    <ListView.ItemTemplate>
                        <DataTemplate x:DataType="models:MediaMetadata">
                            <StackPanel Orientation="Horizontal" Spacing="8">
                                <Image Width="64" Height="90" Source="{x:Bind PosterUrl}" Stretch="UniformToFill"/>
                                <StackPanel>
                                    <TextBlock FontWeight="SemiBold" Text="{x:Bind Title}"/>
                                    <TextBlock Text="{x:Bind Year}" Foreground="Gray"/>
                                    <TextBlock Text="{x:Bind Plot}" TextWrapping="Wrap" MaxWidth="320" MaxHeight="60"/>
                                </StackPanel>
                            </StackPanel>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
                <TextBlock Text="Preview" FontWeight="SemiBold"/>
                <TextBox x:Name="PreviewBox" IsReadOnly="True"/>
                <CheckBox x:Name="MoveToOutput" Content="Move/rename files into output folder" IsChecked="True"/>
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <TextBox x:Name="OutputRootBox" Width="320" PlaceholderText="Output root (leave blank to use dashboard output)"/>
                    <Button Content="Use Dashboard" Click="UseDashboardOutput_Click"/>
                </StackPanel>
                <Button x:Name="AddMappingButton" Content="Add Mapping for Selected Files" Click="AddMappingButton_Click"/>
                <TextBlock Text="Pending renames" FontWeight="SemiBold"/>
                <ListView x:Name="MappingsListView" Height="160">
                    <ListView.ItemTemplate>
                        <DataTemplate x:DataType="x:String">
                            <StackPanel Orientation="Horizontal" Spacing="8">
                                <TextBlock Text="{x:Bind}" Width="300"/>
                            </StackPanel>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <Button x:Name="ApplyMappingsButton" Content="Apply All" Click="ApplyMappingsButton_Click"/>
                    <Button x:Name="UndoLastButton" Content="Undo Last" Click="UndoLastButton_Click"/>
                </StackPanel>
                <TextBlock Text="Edit selected mapping destination:" FontWeight="SemiBold"/>
                <TextBox x:Name="SelectedMappingEditBox" PlaceholderText="Edit destination path or filename"/>
                <Button Content="Update Mapping" Click="UpdateMappingButton_Click"/>
            </StackPanel>
        </Grid>

        <StackPanel Orientation="Horizontal" Spacing="8">
            <CheckBox x:Name="UseLocalMatch" Content="Enable local filename heuristics" IsChecked="True"/>
            <CheckBox x:Name="CreatePlexFolders" Content="Create Plex-style folders" IsChecked="True"/>
        </StackPanel>

        <TextBlock Text="This is a starter UI — launching will open the renamer flow (not implemented)." Foreground="Gray" FontSize="12"/>
    </StackPanel>
</ContentDialog>