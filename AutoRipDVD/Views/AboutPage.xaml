<Page
    x:Class="AutoRipDVD.Views.AboutPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <ScrollViewer>
        <StackPanel Padding="24" Spacing="12">
            <TextBlock Text="About AutoRipDVD" Style="{StaticResource TitleTextBlockStyle}"/>
            <TextBlock Text="A small utility to rip and transcode discs." Style="{StaticResource BodyTextBlockStyle}" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>

            <StackPanel Spacing="8" Padding="0,12,0,0">
                <TextBlock Text="Version" Style="{StaticResource BodyStrongTextBlockStyle}"/>
                <TextBlock x:Name="VersionText"/>
            </StackPanel>

            <StackPanel Spacing="8" Padding="0,12,0,0">
                <TextBlock Text="Detected Tools" Style="{StaticResource BodyStrongTextBlockStyle}"/>
                <TextBlock x:Name="MakeMkvText"/>
                <TextBlock x:Name="HandBrakeText"/>
            </StackPanel>

            <Button Content="Close" HorizontalAlignment="Right" Click="Close_Click"/>
        </StackPanel>
    </ScrollViewer>
</Page>
